import React from 'react';
import LoginCard from '../components/LoginCard';

export default function Login() {
    return (
        <div className='login' >
            <LoginCard />
        </div>
    );
}